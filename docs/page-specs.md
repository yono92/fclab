# FCLab 페이지별 상세 스펙

---

## 1. 홈페이지 (`/`)

### 목적
첫 방문 유저를 검색으로 전환. 터미널/사이버펑크 컨셉으로 "통계 기반 분석 서비스"를 즉시 전달.

### 레이아웃 (구현 완료)

```
┌─────────────────────────────────────────────────────────────┐
│ [Header] FCLab 로고 ─────────────────────────────── [/meta] │
├─────────────────────────────────────────────────────────────┤
│ [매트릭스 코드 레인 Canvas + 그리드 배경 + CRT 스캔라인]      │
│                                                             │
│  FCLab (로고 + float 애니메이션 + glow blur)                 │
│  # ai 추측이 아닌, 통계적 근거로 증명하는 플레이 분석          │
│                                                             │
│  ┌─ SYS.status: ONLINE · uptime · mem · cpu · ping ─┐      │
│  └──────────────────────────────────────────────────┘      │
│                                                             │
│  +── $ nickname_input: ──────────────────────────────+      │
│  │ > [닉네임을 입력하세요 _]                   [분석]   │      │
│  │ $ fc_online nick input -> initiate_analysis       │      │
│  +──────────────────────────────────────────────────+      │
│                                                             │
│  ┌─ 터미널 프리뷰 (한줄씩 타이핑 애니메이션) ──────────┐      │
│  │ $ fclab analyze --nick nickname --limit 20        │      │
│  │ > connecting to nexon_api... [200 OK]             │      │
│  │ ✓ win_rate: 62.0%  ci_95: [48.1%, 74.4%]         │      │
│  │ → report_generated. opening dashboard...          │      │
│  └──────────────────────────────────────────────────┘      │
│                                                             │
│  +─ STAT ─────+ +─ RULE ─────+ +─ TREND ────+             │
│  │ 통계 근거   │ │ 액션 제안   │ │ 변화 추적   │             │
│  │ hover→code │ │ hover→code │ │ hover→code │             │
│  +────────────+ +────────────+ +────────────+             │
│                                                             │
│  [ HOW IT WORKS ]                                          │
│  (1) ===> (2) ===> (3) ===> (4)                            │
│                                                             │
│  +─9─+ +─15─+ +─5─+ +─95%─+  (통계 카운트 카드)            │
│  └───┘ └────┘ └───┘ └─────┘                               │
├─────────────────────────────────────────────────────────────┤
│ [Footer] FCLab · Nexon OpenAPI · Not affiliated with Nexon  │
└─────────────────────────────────────────────────────────────┘
```

### 배경 이펙트
- **매트릭스 코드 레인** (`MatrixRain`): Canvas, 통계/코드 토큰 폭포. 마우스 반경 200px 내 가속+밝기 증가
- **그리드 배경** (`grid-bg`): 40px 간격 녹색 격자
- **CRT 스캔라인** (`Scanlines`): 2px 간격 반투명 오버레이
- **히어로 그라디언트**: 상단 방사형 녹색 글로우

### 인터랙션
- 검색바 Enter 또는 버튼 클릭 → `router.push(/player/${nickname})`
- 빈 닉네임 submit 시 → 입력 필드 흔들림 애니메이션 (animate-shake)
- 피처 카드 hover → 코드 실행 결과 타이핑 이펙트 (TypingText)
- 시스템 상태바 2초 tick 업데이트

### OG 이미지
`opengraph-image.tsx`로 동적 생성. 다크 배경 + 그리드 + 터미널 박스 + FCLab 로고. 링크 공유 시 자동 표시.

### 기술
- Client Component ("use client" — 애니메이션/인터랙션 다수)
- 별도 DB 호출 없음 (정적 콘텐츠)

---

## 2. 유저 대시보드 (`/player/[nickname]`)

### 목적
유저의 플레이를 통계적으로 분석하여 "나는 어떤 플레이어인지, 뭘 바꾸면 좋은지" 답변.

### URL 구조
- `/player/홍길동` → 기본 (공식경기, 최근 20경기)
- 매치타입/경기수는 쿼리 파라미터: `?matchtype=50&limit=20`

### 섹션별 상세

#### 섹션 A: 헤더 + 매치타입 필터칩
```
닉네임 (Lv.32)                                    [최근 20경기 ▼]

🏆공식(47)  📋감독(12)  ⚽친선(3)  🎮볼타(0)      ← 필터칩
                                     ↑ disabled
```
- 닉네임: `UserBasic.nickname`
- 레벨: `UserBasic.level`
- 매치타입 필터칩:
  - 유저의 매치타입별 경기 수를 먼저 조회 → 칩에 경기 수 표시
  - 경기 0인 모드는 disabled (회색, 클릭 불가)
  - 기본 선택: 가장 경기 수가 많은 모드
  - 클릭 시 해당 모드로 데이터 전환
  - 아이콘: 공식🏆, 감독📋, 친선⚽, 볼타🎮, 기타⚙️
  - 모바일: 가로 스크롤 가능
- 경기수 셀렉터: 10, 20, 50, 100

#### 섹션 B: 신뢰도 배지 (TrustBadge)
```
📊 20경기 · 공식경기 · 3/15~4/01 · 갱신 4/02 13:00
██████████░░ 충분
```

#### 섹션 C: 종합 요약
```
┌─────────────────────────────────┬─────────────────────────┐
│     [도넛 차트]                  │ 역대 최고 등급            │
│   13승 3무 4패                   │ 🏆 챔피언스              │
│    승률 65%                      │ 공식경기 기준             │
│                                  │                         │
│  평균 점유율: 52.3%              │ 플레이 스타일             │
│  경기당 골: 1.8                  │ [5축 미니 레이더]         │
│  경기당 유효슛: 3.2              │ "빌드업형" (78/100)       │
└─────────────────────────────────┴─────────────────────────┘
```

데이터:
- 승/무/패: `match_user_stats` WHERE ouid AND match_result GROUP BY
- 역대 최고: `user_max_divisions` WHERE ouid
- 플레이 스타일: `classifyPlayStyle()` 결과

#### 섹션 D: 분석 탭

**탭 1: 슈팅**
| 위치 | 컴포넌트 | 데이터 |
|------|---------|--------|
| 좌측 | ShootingHeatmap | match_shoot_details WHERE ouid |
| 우측 | PercentileGauge × 5 | 유효슈팅률, 골전환율, 박스내비율, 헤딩골비율, PK성공률 |
| 하단 | 존별 분석 테이블 | shoot_details를 6구역으로 분류 집계 |

계산 공식:
```
유효슈팅률 = SUM(effective_shoot_total) / SUM(shoot_total) × 100
골전환율 = SUM(goal_total) / SUM(shoot_total) × 100
박스내비율 = SUM(shoot_in_penalty) / SUM(shoot_total) × 100
헤딩골비율 = SUM(goal_heading) / SUM(goal_total) × 100
PK성공률 = SUM(goal_penalty_kick) / SUM(shoot_penalty_kick) × 100
```

**탭 2: 패스**
| 위치 | 컴포넌트 | 데이터 |
|------|---------|--------|
| 좌측 | 도넛 차트 (PassDistribution) | 숏/롱/스루/로빙/그라운더 비율 |
| 우측 | PercentileGauge × 5 | 전체/숏/롱/스루/로빙 성공률 |
| 하단 | 빌드업 성향 텍스트 분석 | 규칙 기반 문장 생성 |

계산 공식:
```
전체 패스 성공률 = SUM(pass_success) / SUM(pass_try) × 100
숏패스 비율 = SUM(short_pass_try) / SUM(pass_try) × 100
(각 유형 동일 패턴)
```

**탭 3: 수비**
| 위치 | 컴포넌트 | 데이터 |
|------|---------|--------|
| 좌측 | PercentileGauge × 5 | 태클성공률, 블록성공률, 파울, 옐로, 레드 |
| 우측 | ConcededTimeHistogram | 상대 shoot_details의 goalTime 분포 |

실점 시간대 계산:
```sql
-- 상대의 골 시간을 15분 구간으로 분류
SELECT (goal_time / 900) * 15 as bucket, COUNT(*) as goals
FROM match_shoot_details
WHERE match_id IN (내 매치들) AND ouid != :myOuid AND result = 1
GROUP BY bucket ORDER BY bucket;
```

**탭 4: 선수**
| 위치 | 컴포넌트 | 데이터 |
|------|---------|--------|
| 전체 | 테이블 | match_player_stats 집계 → TOP 10 |

```sql
SELECT sp_id, 
  COUNT(*) as appearances,
  AVG(goal) as avg_goal,
  AVG(assist) as avg_assist,
  AVG(sp_rating) as avg_rating
FROM match_player_stats WHERE ouid = :ouid
GROUP BY sp_id ORDER BY appearances DESC LIMIT 10;
```

선수명: `meta_players` JOIN으로 해결.
포지션: `meta_positions` JOIN.
시즌 아이콘: `spId / 1000000` → seasonId → `meta_seasons.seasonImg`.

#### 섹션 E: 액션 제안

```
┌─ 💡 플레이 개선 제안 ────────────────────────────────────────┐
│                                                              │
│  🔴 [HIGH] 박스 안 침투 빈도를 높이세요                        │
│     근거: 박스 내 슈팅 비율 23.5% (상위권 평균 41%)             │
│     20경기 기준 · 95% CI: 18%~30%                            │
│                                                              │
│  🟡 [MED] 스루패스 타이밍을 신중하게 선택하세요                  │
│     근거: 스루패스 성공률 42.1%, 비중 25% (높은 시도 대비 낮은 성공)│
│                                                              │
│  🟢 [LOW] 롱패스 성공률이 낮습니다 (38.2%)                     │
│     숏패스 빌드업을 유지하거나 롱패스 연습을 권장합니다           │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

규칙 평가: `evaluateRules(userStats)` → 최대 5개, priority 순.

#### 섹션 F: 최근 매치 리스트

```
┌─ 최근 매치 ──────────────────────────────────────────────────┐
│ 날짜    │ 모드   │ 결과  │ 점유  │ 슈팅(유효) │ 패스% │ 평점  │
│ 4/01   │ 공식   │ 2:1 승│ 54%  │ 5(3)      │ 87%  │ 7.2  │
│ 3/31   │ 공식   │ 0:3 패│ 38%  │ 2(1)      │ 72%  │ 5.1  │
│ 3/30   │ 공식   │ 1:1 무│ 51%  │ 4(2)      │ 81%  │ 6.5  │
│ ...                                                          │
│ ⚠ 3/28 경기는 평소와 크게 다릅니다 (이상치)                    │
└──────────────────────────────────────────────────────────────┘
```

- 이상치 경기: `detectOutliers()` 결과로 플래그
- 각 행 클릭 → `/player/[nickname]/match/[matchId]`

### 에러 상태

| 상황 | 표시 |
|------|------|
| 닉네임 없음 (API 404) | "해당 닉네임의 유저를 찾을 수 없습니다" + 검색 필드 |
| API 에러 (500, 429) | "일시적 오류. 잠시 후 다시 시도해주세요" + 재시도 버튼 |
| 경기 없음 | "최근 {매치타입} 경기가 없습니다. 다른 매치 타입을 선택해보세요" |
| 게임 점검 중 | "현재 FC Online 데이터가 점검 중입니다" |

### 로딩 상태 (TerminalLoading 공통 컴포넌트)
모든 페이지에서 터미널 스타일 로딩 UI 통일:
- 원형 프로그레스 링 (0~100%)
- 터미널 윈도우 (macOS 타이틀바 + 스텝별 로그)
- 각 스텝 완료 시 ✓ 표시, 현재 스텝은 블링킹 커서
- 하단 프로그레스 바 (녹색 글로우)
- 대시보드: 7스텝 (connecting → fetching → computing → evaluating → classifying → generating → building)
- 매치 상세: 5스텝
- 트렌드: 5스텝
- 비교: 5스텝

---

## 3. 매치 상세 (`/player/[nickname]/match/[id]`)

### 목적
개별 경기를 심층 분석. 양쪽 유저 비교.

### 레이아웃

```
┌─────────────────────────────────────────────────────────────┐
│ ← 돌아가기   2024-04-01 공식경기                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  [나] 닉네임A  2 : 1  닉네임B [상대]                          │
│       승                  패                                 │
│                                                             │
│  ── 경기 요약 ──────────────────────────────────────────── │
│  │ 지표        │ 나     │ 상대   │                           │
│  │ 점유율      │ 54%   │ 46%   │ ← 바 차트로 시각화          │
│  │ 슈팅(유효)   │ 5(3)  │ 4(2)  │                           │
│  │ 패스 성공률  │ 87%   │ 79%   │                           │
│  │ 태클 성공률  │ 82%   │ 71%   │                           │
│  │ 파울        │ 1     │ 3     │                           │
│  │ 코너킥      │ 3     │ 1     │                           │
│  │ 오프사이드   │ 0     │ 2     │                           │
│                                                             │
│  ── 슈팅 히트맵 ─────────────────────────────────────────  │
│  ┌──────────────────┐  ┌──────────────────┐                │
│  │  [나의 슈팅 위치]  │  │  [상대 슈팅 위치]  │                │
│  │  ShootingHeatmap  │  │  ShootingHeatmap  │                │
│  └──────────────────┘  └──────────────────┘                │
│                                                             │
│  ── 골 타임라인 ─────────────────────────────────────────  │
│  0분 ──────── 32분(나) ──── 56분(상대) ── 78분(나) ── 90분  │
│                                                             │
│  ── 선수별 퍼포먼스 ─────────────────────────────────────  │
│  │ 선수      │ 포지션 │ 골 │ 어시 │ 패스% │ 평점 │           │
│  │ 메시(TOTY)│ ST    │ 1  │ 1   │ 88%  │ 8.2 │           │
│  │ 지단     │ CAM   │ 1  │ 0   │ 91%  │ 7.8 │           │
│  │ ...                                                     │
│                                                             │
│  ⚠ 이상 지표: 이 경기의 점유율(54%)은 평소(48%)보다 높습니다   │
└─────────────────────────────────────────────────────────────┘
```

### 데이터
- `matches` WHERE match_id
- `match_user_stats` WHERE match_id (양쪽 유저)
- `match_shoot_details` WHERE match_id (양쪽)
- `match_player_stats` WHERE match_id AND ouid = 나

---

## 4. 나 vs 랭커 비교 (`/player/[nickname]/compare`) — 구현 완료

### 목적
내 선수별 스탯을 랭커 평균과 직접 비교. "뭐가 부족한지" 명확히 보여줌.

### 핵심 데이터 흐름
```
1. 내 주요 사용 선수 TOP 10 (match_player_stats 집계)
2. 각 선수의 랭커 평균 스탯 (ranker_stats WHERE sp_id AND sp_position)
3. 차이 계산 + 효과 크기(Cohen's d)로 유의미한 차이 판별
```

### 선수별 비교 카드
```
메시 (TOTY) — ST — 18경기 출전

[레이더 차트: 나(실선) vs 랭커(점선)]

│ 지표       │ 나    │ 랭커  │ 차이   │ 효과크기 │ 판정    │
│ 경기당 골  │ 0.8   │ 1.2  │ -0.4  │ d=0.8   │ ⚠ 큰차이│
│ 유효슈팅률 │ 55%   │ 68%  │ -13%p │ d=1.1   │ ⚠ 큰차이│
│ 패스성공률 │ 89%   │ 85%  │ +4%p  │ d=0.3   │ ✅ 유사 │
│ 평점       │ 6.8   │ 7.4  │ -0.6  │ d=0.6   │ ⚠ 차이  │

💡 가장 개선 필요: 유효슈팅률 (효과 크기 1.1 — 매우 큰 차이)
```

### 효과 크기 판정 기준
- |d| < 0.2: "✅ 유사" (차이 무시 가능)
- 0.2 ≤ |d| < 0.5: "⚡ 약간 차이"
- 0.5 ≤ |d| < 0.8: "⚠ 차이"
- |d| ≥ 0.8: "⚠ 큰 차이"

---

## 5. 변화 추적 (`/player/[nickname]/trend`) — 구현 완료

### 목적
"지난주보다 나아졌는가?" 답변. 재방문 동기 생성.

### 핵심 조건
- 최소 4경기 이상 DB 이력이 있어야 의미 있음 (Supabase match_user_stats 기준)
- 데이터 부족 시: 에러 메시지 + 대시보드 돌아가기 링크

### 데이터
- 검색할 때마다 `match_user_stats`에 시계열 데이터 축적
- 기간별 집계: `v_user_recent_stats`를 기간 필터로 2번 호출

### 차트
- 승률 가중이동평균 라인 차트 (weightedMovingAverage)
- 핵심 지표 변화 테이블 (이전 vs 현재 vs 변화량)
- 트렌드 해석 텍스트 (규칙 기반)

---

## 6. 메타 대시보드 (`/meta`) — 미구현 (스텁만 존재)

### 목적
현재 메타를 랭커와 일반 유저로 분리해서 보여줌.

### 핵심 차별화
다른 사이트는 랭커 메타 = 전체 메타로 취급. FCLab은 분리.

### 데이터
```sql
-- 랭커 메타: ranker_stats 집계
-- 일반 유저 메타: match_player_stats 집계

-- 포지션별 인기 선수 (랭커)
SELECT sp_id, sp_position, COUNT(*) as usage
FROM ranker_stats
WHERE match_type = 50
GROUP BY sp_id, sp_position
ORDER BY usage DESC;

-- 포지션별 인기 선수 (일반)
SELECT sp_id, sp_position, COUNT(DISTINCT match_id) as usage
FROM match_player_stats
WHERE match_id IN (SELECT match_id FROM matches WHERE match_type = 50)
GROUP BY sp_id, sp_position
ORDER BY usage DESC;
```
